<form>
  <label>Name:</label>
  {{input type='text' value=newSoda.name}}
  <label>Flavor:</label>
  {{#power-select selected=newSoda.flavor options=flavors onchange=(action "setFlavor") as |flavor|}}
    {{flavor.name}}
  {{/power-select}}
</form>
